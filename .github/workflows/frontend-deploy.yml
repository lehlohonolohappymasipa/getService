name: Build and Deploy Frontend to Azure Static Web Apps

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        working-directory: ./getservice-frontend
        run: npm ci

      - name: Build
        working-directory: ./getservice-frontend
        run: npm run build

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_FRONTEND }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_FRONTEND }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_FRONTEND }}

      - name: Deploy to Azure Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          app_location: getservice-frontend
          output_location: getservice-frontend/dist
          azure_static_web_apps_api_token: ${{ secrets.AZURE_CLIENT_ID_FRONTEND }}
